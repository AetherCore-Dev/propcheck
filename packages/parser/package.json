{
  "name": "@propcheck/parser",
  "version": "0.1.0",
  "description": "AST extraction for propcheck — function signatures, types, docstrings",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "node --test dist/**/*.test.js"
  },
  "dependencies": {
    "@propcheck/common": "*"
  },
  "license": "MIT"
}
