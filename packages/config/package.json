{
  "name": "@propcheck/config",
  "version": "0.1.0",
  "description": "Configuration loading for propcheck",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@propcheck/common": "*"
  },
  "license": "MIT"
}
